# cargo-audit configuration (RustSec)
# Looked up from `.cargo/audit.toml` (project) or `~/.cargo/audit.toml`.
# https://docs.rs/cargo-audit/latest/cargo_audit/

[advisories]
ignore = [
  # No fixed rsa release. Present via sqlx mysql/postgres lockfile edges; we do
  # not use the rsa crate for JWT/DPoP (jsonwebtoken uses aws_lc_rs). Aligns
  # with osv-scanner.toml. Revisit when sqlx drops rsa or rsa ships a fix.
  # (sqlx 0.9 + Rust 1.94 may clear the edge — not yet on this toolchain.)
  "RUSTSEC-2023-0071",
]
