{
  "name": "@opensesame/browser-extension",
  "version": "0.1.0",
  "description": "WXT browser extension that reports Host API health, daemon reachability and a sync cursor, and never exposes a secret to a web page",
  "private": true,
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "test": "node --test tests/pact.test.mjs",
    "test:e2e": "playwright test",
    "test:install": "playwright install chromium"
  },
  "dependencies": {
    "@opensesame/api-client": "workspace:*",
    "@opensesame/client-core": "workspace:*",
    "@opensesame/os-domain": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "1.55.1",
    "typescript": "5.8.3",
    "wxt": "0.20.6"
  }
}
