# `@opensesame/web-console`

Stub package reserved for a future Next.js console.

**Use [`apps/console`](../console) today** — the Vite Identity console for:

- Sign in / provisional session
- Authorize CLI (device code)
- Claim ownership
- Task access inspection

```bash
pnpm --filter @opensesame/console dev
```
