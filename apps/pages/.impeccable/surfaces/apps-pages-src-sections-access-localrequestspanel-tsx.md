# Local requests

## Direction contract

THESIS: Create and decide real encrypted application requests without a backend; no placeholder cards.

OWN-WORLD: Existing Access panels, neutral paper, ink actions, teal focus, native fields and wrapped public references.

STORY: Choose an authenticated requester and registered application, state scopes and reason, then review an exact pending request and approve or deny with a bound passkey.

FIRST VIEWPORT: Local requests heading with New request and Reload commands; truthful count and flat request rows below. Inline forms preserve drafts and restore the initiating control after closure.

FORM: Code-led Operate extension of Access; no seed or comp for this constrained local addition. Pending, approved, denied, expired and withdrawn states remain distinct.

FINISH: Prior reviewer disposition: ship at panel/fix scope only. The material stale-selection finding was resolved: review reads the current record by ID, expiry/external decisions remove obsolete approval controls, active focus is retained, and removal restores a useful fallback. Prior scoped evidence: five real encrypted-store UI regressions passed; the real-browser Requests journey passed at 1280px and 390px for creation, request-bound passkey approval/denial, withdrawal/history removal and expiry while review is open. This remains prior panel evidence, not a new visual approval or completion of broader IAM.

POPUP CONNECTION: LocalIssuerChannel now creates a transaction-bound application request, obtains a fresh request-bound passkey decision, consumes approval once, and issues through the existing PKCE code flow. The transaction digest binds client, callback, scopes, state, nonce, PKCE and agent. Members may consent only to their own bound sign-in; manual and agent requests still require an owner/admin. Shared localRequestMemberMayDecide eligibility drives both review controls and enforcement.

VALIDATION UPDATE: 31 focused tests across four files, Pages typecheck/build, scoped anti-slop, design lint and the structural gate pass. The final rebuilt Chromium run after the approval-key issuance fence completed with exit 0: all ten journeys passed at 1280px and 390px. The latest 31-test focused rerun also completed with exit 0. Full IAM and full integrated verification remain incomplete. No new screenshot, asset, layout or visual approval is claimed by this documentation refresh.

DOCUMENTATION: Root DESIGN.md → Local requests and .impeccable/design.json → Local Requests record the implemented reusable pattern within the incumbent world. No new tokens or shipping raster assets.

EVIDENCE: Browser captures under apps/pages/.impeccable/review/ are verification artifacts of the implemented panel, not product imagery: local-request-{form,form-controls,review,review-controls,expired,expired-controls}-{1280,390}.png. Existing application typography, controls and CSS supply the rendered visuals; no generated or external imagery was added.
