/**
 * The immutable distribution contract this build was emitted with
 * (ownership.md §4.6). Same lazy-import arrangement as `module-table.ts`.
 */
export { DISTRIBUTION } from "virtual:opensesame-distribution";
