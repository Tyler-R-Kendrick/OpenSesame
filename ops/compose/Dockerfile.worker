FROM rust:1.88-bookworm AS build
WORKDIR /src
COPY . .
RUN cargo build --release -p opensesame-worker
FROM debian:bookworm-slim
RUN useradd -u 10001 -m app
COPY --from=build /src/target/release/opensesame-worker /usr/local/bin/
USER 10001
ENTRYPOINT ["opensesame-worker"]
