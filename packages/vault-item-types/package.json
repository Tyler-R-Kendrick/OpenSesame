{
  "name": "@opensesame/vault-item-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vault item type parser, registry and the embedded built-in definitions from marketplace/item-types/builtin (ADR 0087)",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "generate": "node scripts/emit-definitions.mjs"
  },
  "dependencies": {
    "@opensesame/os-domain": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.15.29",
    "typescript": "5.8.3",
    "vitest": "4.1.11"
  }
}
