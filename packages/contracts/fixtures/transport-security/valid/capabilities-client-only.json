{
  "kind": "transport_capabilities",
  "input": {
    "native_pem": "supported",
    "managed_certificate": "supported",
    "spiffe_workload_api": {
      "unsupported": {
        "reason": "no workload api socket configured"
      }
    },
    "browser_managed_external": {
      "unsupported": {
        "reason": "a native runtime has no browser certificate store to consume"
      }
    },
    "browser_vault_key_injection": {
      "unsupported": {
        "reason": "a browser cannot attach a vault-held key to its TLS handshake; certificates a browser presents are provisioned outside the app"
      }
    },
    "client_presents_certificate": true,
    "server_enforces_certificate": false
  },
  "expect": {
    "ok": true,
    "canonical": {
      "native_pem": "supported",
      "managed_certificate": "supported",
      "spiffe_workload_api": {
        "unsupported": {
          "reason": "no workload api socket configured"
        }
      },
      "browser_managed_external": {
        "unsupported": {
          "reason": "a native runtime has no browser certificate store to consume"
        }
      },
      "browser_vault_key_injection": {
        "unsupported": {
          "reason": "a browser cannot attach a vault-held key to its TLS handshake; certificates a browser presents are provisioned outside the app"
        }
      },
      "client_presents_certificate": true,
      "server_enforces_certificate": false
    }
  }
}
