/** Interactive configuration document bound (C-DOCUMENT). */
export const MAX_DOCUMENT_BYTES = 256 * 1024;

/** Nested mapping/sequence depth. */
export const MAX_DOCUMENT_DEPTH = 32;

/** Top-level plus nested collection entries. */
export const MAX_COLLECTION_ENTRIES = 4096;

/** Diagnostics retained per draft. Overflow fails closed. */
export const MAX_DIAGNOSTICS = 128;
