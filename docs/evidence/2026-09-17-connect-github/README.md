# Connect GitHub without a Host — journey only

The capture journey for the change that let Pages connect GitHub (and resume a
guest session) with no Host behind it. Only `journey.json` was committed; the
composed sheets were not, so this directory shows how to regenerate them
rather than what they showed. Regenerate with
[`skills/visual-evidence`](../../../skills/visual-evidence/SKILL.md).
