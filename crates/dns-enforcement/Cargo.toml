[package]
name = "opensesame-dns-enforcement"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "DNS-layer enforcement against Blocky: per-unit isolated domain filtering, allowances that only narrow, and truthful DNS-only coverage"

[lib]
name = "opensesame_dns_enforcement"
path = "src/lib.rs"

[dependencies]
chrono = { workspace = true }
reqwest = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
url = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
